type Score
  @model
  @auth(
    rules: [
      { allow: public, provider: apiKey, operations: [read] }
      { allow: groups, groups: ["admin"] }
    ]
  ) {
  id: ID!
  game: String!
  sgScore: Float!
  niScore: Float!
  mgScore: Float!
  fourScore: Float
}

# Atomic, server-side increment for a single score field.
# Backed by a custom DynamoDB UpdateItem ("ADD") resolver so concurrent
# +/- edits no longer clobber each other (the old client-side
# read-modify-write was racy). Auth is enforced in the resolver VTL
# (admin Cognito group only), matching the @model write rules above.
type Mutation {
  incrementScore(id: ID!, field: String!, delta: Int!): Score
}
